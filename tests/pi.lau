-- Gregory-Leibniz series
func calcPi(accuracy)
	varol num = 0
	varol denom = 1
	
	for i = 1, accuracy do
		if i % 2 == 1 then
			num += (4/denom)
		else
			num -= (4/denom)
		end
		denom += 2
	end
	
	return num
end

printn(calcPi(10))